---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.12.12

inputs:
  - name: dp-search-query

outputs:
  - name: build

caches:
  - path: go/

run:
  path: dp-search-query/ci/scripts/build.sh
